import {Layout} from "../layout/Layout";
import {RestaurantPage} from "../restaurant-page/RestaurantPage";

export const App = () => {
    return (
        <Layout>
            <RestaurantPage />
        </Layout>
    );
};